[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "trustbase-hvac-design-engine"
version = "0.1.0"
description = "可覆核 HVAC 計算基礎與原始設計基準對照"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
reference = ["pypdf==6.10.0"]

[tool.setuptools.packages.find]
include = ["hvac_engine*"]
